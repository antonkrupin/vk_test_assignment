import React from 'react';

import './StartButton.css';

const StartButton = () => {
  return (
    <button
      type="button"
      aria-label="start-button"
    />
  );
};

export default StartButton;
